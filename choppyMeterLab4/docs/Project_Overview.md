# 📚 Project Overview: Automated Trading & SMC Analysis System

ไฟล์นี้รวบรวมสรุปฟังก์ชันและคุณสมบัติของทุกโมดูลในระบบ เพื่อใช้เป็นคู่มืออ้างอิงฉบับสมบูรณ์ (Documentation)

---

## 1. 📁 กลุ่มระบบเทรดและเชื่อมต่อ (Core & API)

### 🔹 trader.js - ระบบเทรดอัตโนมัติ
* **ฟังก์ชันหลัก**: `init()`, `startTrading()`, `executeTrade()`, `processTradeResult()`
* **ฟีเจอร์**: จัดการกลยุทธ์ Martingale/Fixed Stake, ตรวจสอบจุดเข้าเทรดที่วินาทีที่ 0 และติดตามผลกำไรขาดทุนแบบ Real-time

### 🔹 deriv.js - ตัวเชื่อมต่อ API
* **ฟังก์ชันหลัก**: `connect()`, `getHistory()`, `subscribeData()`
* **ฟีเจอร์**: จัดการ WebSocket พื้นฐานกับ Deriv, ดึงแท่งเทียนย้อนหลัง และรับข้อมูลราคา Tick ล่าสุด

### 🔹 details.js - ระบบจัดการหน้าเทรด
* **ฟังก์ชันหลัก**: `renderTradePanels()`, `updateAnalysis()`, `updateClock()`
* **ฟีเจอร์**: ควบคุมหน้าจอแสดงผลรายสินทรัพย์, ระบบแนะนำ Action (CALL/PUT) และการล็อคคำสั่งเทรด

---

## 2. 📊 กลุ่มสมองและการคำนวณ (Analysis & Logic)

### 🔹 indicators.js - คลังสูตรเทคนิค
* **ฟังก์ชันหลัก**: `sma()`, `ema()`, `rma()`, `hma()`, `rsi()`, `atr()`, `adx()`, `ci()`
* **ฟีเจอร์**: รวมสูตรคำนวณมาตรฐาน (RMA, TR) สำหรับใช้ในการวิเคราะห์ความผันผวนและเทรนด์

### 🔹 clsAnalysisGenerator.js - ตัวประมวลผลเชิงลึก
* **ฟังก์ชันหลัก**: `generate()`, `getSummary()`, `_analyzeTrend()`, `_checkCrossover()`
* **ฟีเจอร์**: แปลงแท่งเทียนดิบเป็นสถิติ, ตรวจจับการตัดกันของ EMA และค้นหาแท่งเทียนที่ผิดปกติ (Abnormal)

### 🔹 SMCIndicator.js & Standalone - ตัววิเคราะห์โครงสร้างตลาด
* **ฟังก์ชันหลัก**: `_detectSwingPoints()`, `_detectStructure()`, `_detectOrderBlocks()`, `_detectFairValueGaps()`
* **ฟีเจอร์**: วิเคราะห์ BOS, CHoCH, OB, FVG และ Liquidity ระดับสถาบัน แบ่งเป็นระดับ Internal และ Swing

---

## 3. 🎨 กลุ่มแสดงผลและ UI (Visualization & UI)

### 🔹 mainV4.js & Part 2 - ศูนย์กลาง Dashboard
* **ฟังก์ชันหลัก**: `createChart()`, `refreshAll()`, `renderSelectedAnalysis()`, `saveSettings()`
* **ฟีเจอร์**: แสดงผล 8-10 สินทรัพย์พร้อมกัน, จัดการ Settings ของ EMA/ATR และบันทึกค่าลง LocalStorage

### 🔹 SMCChartRenderer.js - ตัววาดกราฟิก SMC
* **ฟังก์ชันหลัก**: `renderStructures()`, `renderOrderBlocks()`, `renderPremiumDiscountZone()`
* **ฟีเจอร์**: เปลี่ยนข้อมูล SMC เป็นเส้นและโซนสีบน LightweightCharts พร้อมระบบจางสีเมื่อโซนถูกใช้ (Mitigated)

### 🔹 meter.js - เข็มไมล์ความผันผวน
* **ฟังก์ชันหลัก**: `setValue()`, `drawGauge()`, `animate()`
* **ฟีเจอร์**: แสดงระดับ Choppiness Index เป็นรูปเข็มไมล์ แบ่งโซนสี เขียว-เหลือง-แดง อย่างสวยงาม

### 🔹 choppiness-indexV3.js - ห้องทดลองกลยุทธ์
* **ฟังก์ชันหลัก**: `createCrossoverZones()`, `updateIndicators()`, `addCustomMarker()`
* **ฟีเจอร์**: ระบบ Lab สำหรับ Backtest, ระบายสีพื้นหลังตามเทรนด์ (Background Zones) และ Sync กราฟ 3 หน้าต่าง

---
*Generated by Gemini - 2026*